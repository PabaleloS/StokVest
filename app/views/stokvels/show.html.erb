<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stokvest Dashboard</title>
    <style>
        body {
            font-family: sans-serif;
            background-color: #f1f5f8;
            color: #333;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            margin: 0;
            padding: 0;
        }

        .parent_container {
            <!--display: flex;
            flex-direction: column;
            min-height: 100vh;-->
            background-color: #f1f5f8;
        }

        .container {
            background-color: #fff;
            padding: 20px;
            border-radius: 10px;
            margin: 20px auto;
            max-width: 900px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        .header {
            position: fixed;
            width: 100%;
            top: 0;
            left: 0;
            z-index: 100;
            background-color: #fff;
            padding: 10px 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-size: 24px;
            font-weight: bold;
            color: #0ea5e9;
            text-shadow: 0 0 5px #0ea5e9;
            text-align: center;  /* Center the logo horizontally */
        }

        .logo img {
            height: 40px;
            margin: 0 auto;  /* Center the logo vertically */
        }

        .profile {
            position: absolute;  /* Move profile to absolute positioning */
            top: 10px;  /* Adjust top position as needed */
            right: 20px;  /* Adjust right position as needed */
            display: flex;
            align-items: center;
        }

        .profile-avatar {
            width: 40px;
            height: 40px;
            background-color: #0ea5e9;
            border-radius: 50%;
        }

        .profile-name {
            font-size: 18px;
            font-weight: bold;
            margin-left: 10px;
        }

        .section {
            margin-top: 80px;  /* Adjust margin to account for header height */
            padding: 20px;
            border-radius: 8px;
            background-color: #f1f5f8;
        }

        .section-title {
            font-size: 16px;
            font-weight: bold;
            margin-bottom: 10px;
        }

        .section-content {
            background-color: #fff;
            padding: 15px;
            border-radius: 8px;
        }

        .balance {
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 10px;
        }

        .balance-amount {
            font-size: 36px;
            font-weight: bold;
            color: #0ea5e9;
        }

        .actions {
            display: flex;
            justify-content: space-between;
            margin-top: 20px;
        }

        .action-button {
            background-color: #0ea5e9;
            color: #fff;
            padding: 10px 20px;
            border-radius: 8px;
            cursor: pointer;
            display: inline-block;
            margin: 5px;
        }

        .action-button:hover {
            background-color: #0a85e9;
        }

        .next-withdrawals {
            margin-top: 20px;
        }

        .next-withdrawal {
            display: flex;
            align-items: center;
            margin-bottom: 10px;
        }

        .next-withdrawal-avatar {
            width: 30px;
            height: 30px;
            background-color: #0ea5e9;
            border-radius: 50%;
            margin-right: 10px;
        }

        .next-withdrawal-name {
            font-size: 16px;
            font-weight: bold;
        }

        .next-contribution-date {
            font-size: 16px;
            font-weight: bold;
            margin-top: 20px;
        }

        .view-contributions {
            background-color: #0ea5e9;
            color: #fff;
            padding: 10px 20px;
            border-radius: 8px;
            cursor: pointer;
            margin-top: 20px;
        }

        .view-contributions:hover {
            background-color: #0a85e9;
        }

        /* Footer styles */
        footer {
            background-color: #333;
            color: #fff;
            padding: 10px;
            text-align: center;
        }

        /* Navbar styles */
        .navbar {
            background-color: #333;
            color: #fff;
            padding: 10px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: fixed;
            bottom: 0;
            left: 0;
            width: 100%;
            z-index: 1;
        }

        .navbar-item {
            margin: 0 10px;
        }

        .navbar-link {
            color: #fff;
            text-decoration: none;
        }
    </style>
</head>
<body>
  <div class="parent_container">

    <div class="container">
        <div class="header">
            <div class="logo">
                <img src="/path/to/your/logo.png" alt="StokVest Logo">
            </div>
        </div>
        <div class="profile">
            <div class="profile-avatar"></div>
            <span class="profile-name"><%=@user.first_name%></span>
        </div>
        <div class="section">
            <h2 class="section-title">Stokvel Details</h2>
            <div class="section-content">
                <p>Stokvel Name: <%= @stokvel.name %></p>
                <%= link_to "Invite", new_stokvel_member_path(@stokvel), class: "action-button" %>
            </div>
        </div>
        <div class="section">
            <h2 class="section-title">Balance</h2>
            <div class="section-content">
                <p class="balance">Balance: R <%= number_with_precision(@stokvel.balance, precision: 2) %></p>
                <div class="actions">
                    <%= link_to "Payout", stokvel_payouts_path(@stokvel), class: "action-button" %>
                    <%= link_to "Top-Up", new_stokvel_contribution_path(@stokvel), class: "action-button" %>
                </div>
            </div>
        </div>
        <div class="section">
            <h2 class="section-title">Next Withdrawals</h2>
            <div class="section-content">
                <% @stokvel.users.first(3).each do |user| %>
                    <div class="next-withdrawal">
                        <div class="next-withdrawal-avatar"></div>
                        <span class="next-withdrawal-name"><%= user.first_name %></span>
                    </div>
                <% end %>
            </div>
        </div>
        <div class="section">
            <h2 class="section-title">Next Contribution Date</h2>
            <div class="section-content">
                <p class="next-contribution-date">Next Contribution Date: <%= @stokvel.next_contribution_date.strftime('%d/%m/%Y') %></p>
                <%= link_to "View Contributions", stokvel_contributions_path(@stokvel), class: "action-button" %>
            </div>
        </div>
        </div>
    </div>
